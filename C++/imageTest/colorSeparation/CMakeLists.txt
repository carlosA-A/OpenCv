cmake_minimum_required(VERSION 2.8)
project( color_separation )
find_package( OpenCV REQUIRED )
add_executable( color_separation color_separation.cpp )
target_link_libraries( color_separation  ${OpenCV_LIBS} )
